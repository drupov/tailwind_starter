/**
 * @file
 * Tailwind starter behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.tailwindStarter = {
    attach (context, settings) {
      console.log('Tailwind starter initialized.');
    }
  };

} (Drupal));
